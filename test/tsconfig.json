{
    "compilerOptions": {
        "module": "CommonJS",
        "moduleResolution": "Node",
        "strict": true,
        "target": "ES2022"
    },

    "include": ["cases/*.test.ts"],
    "exclude": ["expect-failure"]
}
